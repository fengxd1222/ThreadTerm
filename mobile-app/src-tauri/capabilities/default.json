{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "ios-default",
  "description": "Minimal capabilities for the ThreadTerm iOS workspace client. No desktop filesystem or shell.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
